package com.igg.go2.msg;

import com.igg.go2.common.MsgHead;
import com.igg.go2.common.MsgTypes;
import net.skimnerphi.go2.util.ByteArray;

/**
 * Ported by SkimnerPhi using script
 * @author IGG
 */
public class MsgRespUpdatePlayerName extends MsgHead {
  public byte errorCode;
  
  public MsgRespUpdatePlayerName() {
    super();
    super.usSize = this.getLength();
    super.usType = MsgTypes.MSG_RESP_UPDATEPLAYERNAME;
  }
  @Override
  public void readBuf(ByteArray b) {
    super.usSize = b.readShort();
    super.usType = b.readShort();
    
    this.errorCode = b.readByte();
  }
  @Override
  public void writeBuf(ByteArray b) {
    b.setPosition(0);
    
    b.writeShort(super.usSize);
    b.writeShort(super.usType);
    
    b.writeByte(this.errorCode);
    
    b.padSize(super.usSize);
  }
  
  @Override
  public short getLength() {
    return getLength((short)0);
  }
  @Override
  public short getLength(short param1) {
    return 5;
  }
  
  @Override
  public void release() {
    //do nothing
  }
}
